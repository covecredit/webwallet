import { initializationService } from './service';
import type { InitializationState, InitializationOptions } from './types';

export { initializationService };
export type { InitializationState, InitializationOptions };
export default initializationService;